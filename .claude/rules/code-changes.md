# Code Changes

## テスト失敗時の方針

1. プロダクションコードとテストのどちらが間違っているかを先に判断する
2. 最近追加・変更されたコードの意図を git log や差分から読み取る
3. 判断がつかない場合はユーザーに確認する

## 変更の最小化

- 目的と無関係な変更（メソッド順序の入れ替え、フォーマット変更、リネームなど）を混ぜない
- 修正後に `git diff` で意図しない変更が含まれていないか検証する
